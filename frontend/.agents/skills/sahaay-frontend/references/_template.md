# <prefix>-<topic>

<!-- Copy this file to add a rule. Delete these comments.
     A rule with no wrong/right pair is not yet a rule — it is a preference. -->

**One-sentence statement of the rule.**

## Why it matters

Two or three sentences. Tie it to a real consequence — a user who cannot read the screen, a
build that breaks, a wrong number in front of someone making a financial decision. "It's
cleaner" is not a reason.

## Wrong

```tsx
// what someone reaches for by default, and what goes wrong
```

## Right

```tsx
// what to do instead
```

## Notes

Edge cases, exceptions, related rules, the ADR that decided it.
