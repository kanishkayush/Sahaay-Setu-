# Changelog — scheme-domain

## 1.0.0 — 2026-09-07

Initial skill, extracted from `context/domain.json` and the v0 rule engine.

- 8 rules across 3 prefixes: `rule-`, `math-`, `data-`
- Each rule maps to the file implementing it, so a change to the rule and a change to the code
  stay in step
