# <prefix>-<topic>

<!-- Copy this file to add a rule. Delete these comments.
     A rule with no wrong/right pair is not yet a rule — it is a preference. -->

**One-sentence statement of the rule.**

## Why it matters

Two or three sentences tying the rule to a real consequence. For this project that usually
means: a wrong number in front of someone making a financial decision, a screen a user cannot
read, or a build that breaks for the teammate on the other side of the contract.

## Wrong

```ts
// what someone reaches for by default, and what goes wrong
```

## Right

```ts
// what to do instead
```

## Notes

Edge cases, the file that implements it, the ADR that decided it, related rules.
